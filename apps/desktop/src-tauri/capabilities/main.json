{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The only window. No plugins, no network, no file system access from the UI (ADR 0003).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
